<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- В фокусе -->
    <item android:color="@color/orange" android:state_focused="true"/>

    <!-- Не в фокусе -->
    <item android:color="@color/light_orange"/>
</selector>